Just download this boost version from the boost website
DOwnload the binaries (x64 bit, windows)
Install through the install wizard.. and grab the proper libraries and put everything into this directory
Check the cmake scripts to see where they expect the libraries 